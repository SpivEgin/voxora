name: agent_web
version: 1.0.0
description: Pure Dart Web agent interface for VoIP Server

environment:
    sdk: ">=3.0.0 <4.0.0"

dependencies:
    web: ^1.1.1
    web_socket_channel: ^3.0.3
    http: ^1.1.0

dev_dependencies:
    build_runner: ^2.4.0
    build_web_compilers: ^4.0.0
    lints: ^6.1.0
    test: ^1.24.0

# web:
#   compiler:
#     debug: dart2js
#     release: dart2js
